# Server Configuration
PORT=8080
NODE_ENV=production

# For Cloud Run with Cloud SQL
# Use this DATABASE_URL format for Cloud Run (Unix socket connection)
DATABASE_URL="postgresql://${DB_USER}:${DB_PASS}@localhost/${DB_NAME}?host=/cloudsql/${INSTANCE_CONNECTION_NAME}"

# Database Credentials
DB_USER=your-db-user
DB_PASS=your-db-password
DB_NAME=your-database-name

# Cloud SQL Instance Connection Name
INSTANCE_CONNECTION_NAME=YOUR_PROJECT:YOUR_REGION:YOUR_INSTANCE_NAME

# Note: When deploying to Cloud Run, you need to:
# 1. Configure the Cloud SQL connection in the Cloud Run service
# 2. Set up IAM permissions for the Cloud Run service account
# 3. Enable the Cloud SQL Admin API 